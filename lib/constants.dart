// All app-wide constants (strings, keys, etc.)
class AppConstants {
  static const defaultHost = 'localhost';
  static const defaultPort = 1883;
  static const connectionsKey = 'connections';
  // Add more constants as needed
}
